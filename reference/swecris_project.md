# Retrieve details about a project

Given the project identifier, retrieve details about a project.

## Usage

``` r
swecris_project(project_id, format = c("tbl", "object"))
```

## Arguments

- project_id:

  string identifier for the project

- format:

  one of "object" or "tbl"

## Value

R object or tbl with results
